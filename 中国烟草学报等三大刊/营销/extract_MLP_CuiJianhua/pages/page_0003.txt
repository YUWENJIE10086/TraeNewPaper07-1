崔建华等 基于 MLP 主导的卷烟销量预测模型构建及应用研究 135
线需求/基线销量序列 （按品规维度可扩展为 按上述原理构造的基线预测层实现了“多品规共
的预测矩阵，K 为ba品sb规数量）。 享参数+定长多步向量输出+输出端投影”的功能组
ŷt+1:t+H
a）网络结构与前向传播：设在同一地市（或区域） 合：MLP 在同一网络内学习多个品规的共性波动模式
K × H
内共有 K 个卷烟品规，在时刻 t 将销量历史窗口、计 （如周内周期、节令共振），提升长尾品规在数据稀疏
划与供给等结构化特征拼接为输入向量 。记第 条件下的稳定性；同时以向量直接输出保证预测步长
0 层为输入层 ，第 个隐藏层（ d ） 严格固定，并以投影机制保证预测值在有效的范围内，
xt ∈ R
为 (0) ，其中 、 分别为 从而实现可复现、可对接、可审计的基线预测。
h = xt l l =1, …,L − 1
权值(i矩) 阵与偏(置i) 向(i-量1)， (i)为非线性激(活i) 函数(i)。输出
h = φ(W h + b ) W b
层采用线性回归形式 。
φ(·)
为保证预测长度严格固定ba，sb采用“多(L步) 长(L-向1)量直(接L)输
ŷt+1:t+H = W h + b
出”而非逐步递推，从机制上避免出现输出步长不一
致。
b）损失函数、正则化与输出端投影：以监督学习
方式构造样本对 ，采用均方误差（MSE）
作 为 主 损 失 函 数 ：
(xt,yt+1:t+H)
J(θ，)其=中(1 /N)∑t∈D||yt+1为:t+模H型−
2 2
basb (i) (i) (i)
ŷ
参t+数1:t，+H| 为
|2 +
L2
λ
正 ∑i则
||
系
W
数
|
。
|F
针对节令
θ
窗
=
口
W
与供
,b
给扰动引
入的异常点，可将 MSE 替换为 Huber 损失以提升鲁棒
λ
性。为确保基线结果可直接用于补货计算，输出端引
入基础投影，对预测值执行非负与必要的上下限裁剪：
，从而保证数值可用与格
式ba稳sb定。 basb basb
ŷ ←clip (ŷ ,0, U )
c）参数优化与训练-预测流程（图 3）：为将卷烟
图 3 基线预测层 MLP 模型流程
经营计划与供给约束以“可计算”形式植入基线预测，
Fig. 3 Operating process of the MLP model for the baseline
样本构造与特征设计采用场景化扩展：在时间粒度上 prediction layer
以日（或周）为基本单位，将销售、订发货、在途、
（2）策略校正层基于 LLM 模型，用于生成“如
库存、计划分解与达成率等数据对齐到同一时间轴；
何从基线走向实际可执行结果”的校正方案，其输出
在特征构造上以滑动窗口方式提取历史销量序列（长
为结构化指令而非最终销量预测（销量预测结果由“约
度为L的滞后窗口），并拼接计划销售、计划生产、可
束融合输出层”获得）。该层以基线预测层 MLP 模型
供货量、库存结构、缺货/控量标记、节令窗口等结构
的基线预测序列 为输入基准，同时读取业务
化变量形成输入 ；标签 为未来 步的实际销
输入层的计划分解口bas径b 、阶段达成率、供给能力与库
量，用于监督训练。 ŷt+1:t+H
xt yt+1:t+H H 存结构约束、节令窗口与重点策略等信息，模拟围绕
计划目标进行投放调配的逻辑，输出结构化的校正指
令包 。策略校正层输出指令通过约束融合输出层进
行 确 定 性 变 换 得 到 候 选 校 正 序 列
u
并对 c an执d行企
ŷt+1:t+H =
业规则与物b理asb约束的一致性校验后，得到ca带nd硬约束的
clip(α ⊙ ŷt+1:t+H + β,floor,cap) ŷ
可 行 化 过 程 最 小 改 动 优 化 模 型 ：
， 其 中
图 2 基线预测层 MLP 模型结构 2
finai cand
Fig. 2 MLP model structure for the baseline prediction layer ŷ
为企业
=
规
a
则
rg
与
m
物
in
理y
|
约
|y
束
−
构
ŷ
成的
||
可
2 ,
行
s.
域
t.y
，
∈
最
F
终得到长度
F